name: Docker Build and Push to Harbor

on:
  push:
    branches:
      - main  # Change to your desired branch

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set Up Docker
        uses: docker/setup-buildx-action@v3

      - name: Login to Harbor
        run: echo "Aa112233" | docker login habor.analytichpxv3.online/tensedev-api -u tensedev --password-stdin

      - name: Build and Tag Docker Image
        run: |
          docker build -t habor.analytichpxv3.online/tensedev-api:latest .
          docker tag habor.analytichpxv3.online/tensedev-api:latest habor.analytichpxv3.online/tensedev-api:${{ github.sha }}

      - name: Push Docker Image to Harbor
        run: |
          docker push habor.analytichpxv3.online/tensedev-api:latest
          docker push habor.analytichpxv3.online/tensedev-api:${{ github.sha }}